{
  "version": 2,
  "builds": [{ "src": "./index.js", "use": "@vercel/node" }],
  "routes": [
    {
      "src": "/node/(.*)",
      "dest": "/node"
    },
    {
      "src": "/(.*)",
      "dest": "/"
    }
  ],
  "build": {
    "postinstall": "npm rebuild bcrypt --build-from-source"
  }
}
